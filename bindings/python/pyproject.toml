[build-system]
requires = ["setuptools", "pybind11>=2.10"]
build-backend = "setuptools.build_meta"

[project]
name = "logic_sim"
version = "1.0.0"
description = "Digital Logic Circuit Simulation Core"
requires-python = ">=3.10"
